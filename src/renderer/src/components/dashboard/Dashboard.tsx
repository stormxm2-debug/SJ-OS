import HomeDashboard from './home/HomeDashboard'

/**
 * The CEO Home Dashboard — a minimal, fast overview of the company after
 * startup. It reuses the existing dashboard shell and UI primitives without
 * changing the architecture.
 */
export default function Dashboard(): JSX.Element {
  return <HomeDashboard />
}
